allprojects {
    group 'com.ederfmatos'
    version '1.0-SNAPSHOT'

    apply plugin: 'java'

    configurations {
        compileOnly {
            extendsFrom annotationProcessor
        }
    }

    repositories {
        mavenCentral()
    }
}